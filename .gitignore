# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# production
build/

# env
.env
.env.local
.env.local.*
.env.*.local

# What deploy.sh mints or the operator writes beside a deployment's `.env`:
# the host's database and object-store credentials, and the secrets file.
.env.host
.env.secrets

# deploy — the filled-in copies of deploy/*/*.example, and the kubeconfig the
# eks profile mounts. `.env` above already covers each deployment's `.env`.
deploy/idc/.env.aws
deploy/idc/.env.mcp
deploy/idc/kubeconfig
deploy/local/.env.aws

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# testing
coverage/

# logs
npm-debug.log*
pnpm-debug.log*

# prompt files
*.prompt.md
