# dependencies
node_modules/
.pnpm-store/

# next.js
.next/
out/

# production
build/

# env
.env
.env.local
.env.*.local

# deploy — the filled-in copies of deploy/*/*.example, and the kubeconfig the
# eks profile mounts. `.env` above already covers each deployment's `.env`.
deploy/idc/.env.aws
deploy/idc/.env.mcp
deploy/idc/kubeconfig
deploy/local/.env.aws

# misc
.DS_Store
*.pem
*.tsbuildinfo
next-env.d.ts

# testing
coverage/

# logs
npm-debug.log*
pnpm-debug.log*

# prompt files
*.prompt.md
