# AWS credentials for CloudWatch only (the aws Compose profile).
# Copy to .env.aws and fill in; Agent Studio does not read this file.
# --- CloudWatch AWS credentials ---
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# For temporary credentials, also set AWS_SESSION_TOKEN and refresh all three.
# AWS_SESSION_TOKEN=
