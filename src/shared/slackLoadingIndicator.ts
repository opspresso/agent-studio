/**
 * Built-in emoji shown while a Slack edit-in-place reply is unfinished.
 * Custom names render as literal text in workspaces that do not define them;
 * a streamed reply needs none because Slack marks it as still arriving.
 */
export const DEFAULT_LOADING_INDICATOR = ":hourglass_flowing_sand:";
